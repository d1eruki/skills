interface:
  display_name: "Frontend Maintenance"
  short_description: "Audit and safely modernize frontend dependencies"
  default_prompt: "Use $frontend-maintenance to audit this frontend stack and derive compatible maintenance changes."
